{
  "name": "redx-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@ant-design/icons": "^4.7.0",
    "antd": "^4.19.1",
    "awesome-debounce-promise": "^2.1.0",
    "axios": "^0.26.1",
    "classnames": "^2.3.1",
    "esbuild": "^0.15.5",
    "react": "^17.0.2",
    "react-async-hook": "^4.0.0",
    "react-dom": "^17.0.2"
  },
  "devDependencies": {
    "@types/node": "^16.11.24",
    "@types/react": "^17.0.39",
    "@types/react-dom": "^17.0.11"
  },
  "scripts": {
    "build:esbuild": "esbuild src/index.jsx --bundle --minify --sourcemap --outfile=build/app.min.js",
    "build": "mkdir -p build && cp public/* build/ && npm run build:esbuild"
  }
}
